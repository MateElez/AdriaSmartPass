# Email-only lead capture — no database required.
# Outbound "from" address is fixed in code: Adria SmartPass <info@adriasecuritysystems.com>
RESEND_API_KEY=""
# Preferred inbox for new-lead notifications. If empty, NOTIFY_OWNER_EMAIL is used instead (same code path).
LEAD_NOTIFY_EMAIL=""
